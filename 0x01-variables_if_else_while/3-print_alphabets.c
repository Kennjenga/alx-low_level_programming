#include <stdio.h>
/**
 * main - alphabets
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int n = 97;
	int x = 65;

	while (n < 123)
	{
		putchar(n);
		n++;
	}
	while (x < 91)
	{
		putchar(n);
		n++;
	}
	putchar('\n');
	return (0);
}
