#include <stdio.h>
/**
 * main -use printf compile without warning when using the -Wall gcc option
 * Return: 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
